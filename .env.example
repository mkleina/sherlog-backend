# Backend configuration
# Bind address for HTTP server
BIND_ADDR=:8080

# How many previous lines to send along with an error line
CONTEXT_LINES=20

# CORS: set to your frontend origin (e.g., http://localhost:3000)
CORS_ALLOW_ORIGIN=http://localhost:3000

# Google Gemini API key
GOOGLE_API_KEY=your_gemini_api_key_here

# Prompt templates (can be single-line or use \n for newlines)
# Available variables:
#  - {{CONTEXT_LINES}}, {{CONTEXT}}, {{ERROR_LINE}} for PROMPT_TEMPLATE
#  - {{QUESTION}}, {{ISSUE}} for FOLLOWUP_PROMPT_TEMPLATE
# Define possible actions and their args in the prompt so AI returns actionable options.
PROMPT_TEMPLATE=
FOLLOWUP_PROMPT_TEMPLATE=
